Create a complete, usable C application for Linux called Expense Splitter that can be compiled with GCC and run on a Seneca Matrix-style environment. The program must be written strictly in C, not C++, and must use only standard libraries that are normally available on Linux systems used for introductory programming courses. The result must compile cleanly with gcc and run in a terminal. Do not use external libraries, platform-specific features, or anything that would fail on Matrix.

Build the program as a command-line or terminal-based roommate expense splitting tool. It should let the user enter a total bill amount such as rent, groceries, or utilities, enter the number of people involved, and then enter each person’s name. After that, the program must support weighted splitting, where each person can be assigned a percentage of the total instead of forcing an equal split. For example, one roommate may pay 60 percent and another may pay 40 percent. The program must validate the percentages and make sure the total is exactly 100 percent before calculating the results. If the total is not 100 percent, the program must display a clear error and ask the user to enter the values again in a safe way.

The program must calculate exactly how much each person owes and display the results clearly in a readable format with names and dollar amounts rounded properly to two decimal places. Input must be validated carefully so the bill amount cannot be zero or negative, the number of roommates must be valid, and invalid text or numeric input does not crash the program.

Add persistent storage so the program keeps data between runs. Save every completed expense split into a ledger file so that past transactions accumulate instead of being overwritten. Each ledger entry should include a timestamp, the total bill amount, each roommate’s name, each assigned percentage, and each final amount owed. Also store roommate names in a separate file so the user does not have to re-enter the same names every time. When the program starts, it should load any saved roommate data if it exists and allow the user to reuse it. If no saved data exists yet, the program should handle that cleanly and continue normally.

Structure the code professionally and clearly using multiple functions for input, validation, calculations, file loading, file saving, and displaying results. Use arrays and structs where appropriate to keep the code organized and readable. Keep the logic simple, practical, and maintainable, like a strong college-level C assignment that is realistic and actually usable. Make sure file handling is correct, error-checked, and compatible with Linux.

Return only the final complete C source code in a single file. Do not give explanations, summaries, pseudocode, design notes, or markdown. The code must be ready to paste into a file, compile with gcc, and run immediately.